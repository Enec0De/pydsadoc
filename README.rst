PyDSADoc
========

**PyDSADoc** is a Python project for implementing Data Structure and
Algorithms.


Prerequisites
-------------

After the initial cloning, please create a Python virtual environment by
executing the following command::

    python -m venv .venv
    python -m pip install -r ./requirements.txt
